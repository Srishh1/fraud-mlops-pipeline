# Tells Grafana where to find dashboard JSON files to auto-load
apiVersion: 1
providers:
  - name: fraud-mlops
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
